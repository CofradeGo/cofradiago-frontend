export const ROUTES = {
  login: (domain: string) => `/${domain}/login`,
  dashboard: (domain: string) => `/${domain}/dashboard`,
  perfil: (domain: string) => `/${domain}/perfil`,
  usuarios: (domain: string) => `/${domain}/usuarios`,
  hermandad: (domain: string) => `/${domain}/hermandad`,
  hermanos: (domain: string) => `/${domain}/hermanos`,
  cofradia: (domain: string) => `/${domain}/cofradia`,
  papeletas: (domain: string) => `/${domain}/papeletas`,
  notificaciones: (domain: string) => `/${domain}/notificaciones`,
};
